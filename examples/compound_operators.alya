# Compound assignment operators (+=, -=, *=, /=)

let count = 10
say "Initial: {count}"

count += 5
say "After += 5: {count}"

count -= 3
say "After -= 3: {count}"

count *= 4
say "After *= 4: {count}"

count /= 2
say "After /= 2: {count}"
