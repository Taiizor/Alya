# Demonstration of sqrt, pow, and repeat loop in Alya

say "=== Math Built-ins ==="
say sqrt(16)            # 4
say pow(2, 8)           # 256

say "\n=== Repeat Loop ==="
let loops = 0
repeat
    loops += 1
    if loops >= 3
        break
    end
end
say "Completed loops: {loops}"