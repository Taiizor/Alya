# Command-line arguments in Alya
# Usage:
#   alyac run examples/cli_args.alya -- foo bar baz

let cli_arguments = args()
say "=== Command-Line Arguments ==="
say "Argument count: {cli_arguments.len()}"

for arg in cli_arguments
    say "Arg: {arg}"
end
